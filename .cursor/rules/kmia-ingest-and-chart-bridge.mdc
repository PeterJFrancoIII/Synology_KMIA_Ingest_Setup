---
description: Synology KMIA ingest serves the golden master forecast-precision chart pipeline
globs: "**/*"
alwaysApply: true
---

# KMIA ingest → chart bridge

This project feeds the Kalshi KMIA max-temperature research program. The downstream visualization standard is the golden master chart (`chart_kmia_PLUS_mean_median_stability_wind.py`).

Ingest priorities:
1. Raw NDFD + ISD under `/volume2/Data/App_Development/KMIA_Ingest`
2. Processed point CSVs compatible with golden master inputs (VALID_ONLY filtering, lead-time window)
3. Manifest/checksum logging before scaling backfill

Do not treat NDFD as observed data. No live trading. Active compose path: `Docker/kmia-arch-ingest/`.
