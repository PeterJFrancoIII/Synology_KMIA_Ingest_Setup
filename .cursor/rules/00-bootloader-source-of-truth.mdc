---
description: Zero-Drift Bootloader is the mandatory source of truth for all agent work in this repo.
alwaysApply: true
---

# Bootloader source of truth

All AI agents (Cursor AUTO, subagents, Claude Code, background agents) **must** follow:

`0_Developer_Source_Files/AI_System_Architect_Bootloader_Zero-Drift_Build_5.18.26.md`

## Before major work

1. Read bootloader Sections **0–8** (master prompt, laws, Mission Control Packet, drift prevention).
2. Read `0_Developer_Source_Files/MISSION.md`, `0_Developer_Source_Files/current-objective.md`, and **`0_Developer_Source_Files/ROUTING.md`**.
3. Read `0_Developer_Source_Files/AGENTS.md` for project-specific commands and risk classes.
4. For script work: `legion5/ACTIVE_MANIFEST.json` and `ingest/scripts/ACTIVE_MANIFEST.json` — do not grep entire script trees.

## Operating law (summary)

- Mission before code · Plan before edits · Evidence before confidence
- Narrow write scope · One writer per worktree
- Memory in version-controlled files, not chat
- Red-zone (secrets, prod, auth, migrations) requires explicit human approval

## Project-specific rules (also always load)

- `.cursor/rules/console-2-boundaries.mdc`
- `.cursor/rules/kmia-ingest-and-chart-bridge.mdc`
- `.cursor/rules/legion5-processor-defaults.mdc`
- `.cursor/skills/kmia-nas-ingest/SKILL.md` when operating NAS ingest
- `.cursor/skills/kmia-policy-sweep/SKILL.md` when running Legion5 policy sweep
- `.cursor/skills/kmia-nas-deploy/SKILL.md` when deploying NAS runtime from Mac
- `.cursor/skills/kmia-mae-charts/SKILL.md` when building MAE charts on Legion5

## Three-console system

This repo is **Console 2** only. See `docs/architecture/THREE_CONSOLE_ARCHITECTURE.md`. Do not merge Kalshi Main Console (Console 1) or Auto Trader (Console 3) code here.

## Handoff format

Use bootloader Section 0 response format for architect sessions: MISSION, STATE, PLAN, SCOPE, VERIFY, RISKS, HANDOFF.
