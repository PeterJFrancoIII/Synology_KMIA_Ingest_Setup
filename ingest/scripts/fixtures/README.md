# Agent fixtures

Minimal JSON for schema inspection. **Do not use as research data.**

| File | Mimics |
|------|--------|
| `sample_policy_sweep.json` | `policy_sweep_*.json` (one config) |
| `sample_backtest_day.json` | One day from `kalshi_price_backtest_*.json` |

Full runs live on Legion5 `D:\KMIA_Process\kalshi_mirror\` and Mac `Research/.../Kalshi_Price_Backtest/` (gitignored sweeps).
